#!/bin/bash

# Just a small bash script to run the python version of hangman, as it does not to be compiled
# only the first argument is passed along to the script as it can't handle any more anyway
python ./src/main.py $1
